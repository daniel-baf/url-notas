/* UN COMENTARIO DE UNA LINEA */
Main {
    // ignorar esta linea

        /*esto tambien debe ser ignorado*/        
    
    INTEGER a;
    Read a;
    INTEGER b := 2;

    Read a, b;

    /*
        Creo que el problema es la 
        multilinea
        Pero esto no debe
        causar efectos
    */

    If h < 12 Then Read a, b ;
    if z >= a Then Write h;

    j;



    j := 12;

    j := 12 + 2;

    a := .;

    j := (12 + 2)/5 - (h + 1)^(2-1);

}